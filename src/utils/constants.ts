export const CONFIG = Object.freeze({
  BOT_TOKEN: process.env['BOT_TOKEN'] as string,
  APPLICATION_ID: process.env['APPLICATION_ID'] as string,
  PREFIX: process.env['PREFIX'] as string,
});

export const EMBED_COLOR = 0xffff00;
